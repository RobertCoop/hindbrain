---
description: Show hindbrain queue and store status
---
Run `mem stats` and `mem queue` via Bash, then summarize: store size and health, open candidates (recommend save/drop for each), and anything needing my confirmation.
